'use client';

export default function WorkflowPage() {
  return (
    <div className="flex items-center justify-center h-full flex-col gap-4" style={{ color: 'var(--text-muted)' }}>
      <span style={{ fontSize: 56 }}>🔄</span>
      <div className="text-xl font-semibold" style={{ color: 'var(--text-primary)' }}>工作流</div>
      <div className="text-sm">可视化工作流编排功能开发中</div>
    </div>
  );
}
