# OpenAGI MVP 验收标准 v2（M4 版）

> **版本**：v2.0 | **日期**：2026-04-15 | **作者**：M4（MacBook Air M4）
> **v1→v2 改进**：吸收 M2 的量化测试标准（[AUTO]/[MANUAL]标签 + 精确数字）+ 保留 M4 的现状感知（✅/⚠️/❌/🔲）
> **格式说明**：`[A]` = 可自动化测试 | `[M]` = 需人工验证 | 状态标记 = 当前实现情况

---

## 状态标记

| 标记 | 含义 |
|------|------|
| ✅ | 代码完整 + 测试通过 |
| ⚠️ | 核心实现有，边缘或集成缺失 |
| ❌ | 有代码但逻辑错误 |
| 🔲 | 未开始（无代码） |

**MVP 发布门槛**：所有「必须」项无 ❌/🔲，全部达到 ✅ 或 ⚠️

---

## 快速定位：当前最大缺口

| 优先级 | 模块 | 文件路径 | 缺口描述 |
|--------|------|---------|---------|
| 🔴 P0 | ReAct 执行循环 | `cortex/loop/__init__.py` | 空文件，整个 Agent 引擎无法运行 |
| 🔴 P0 | 权限熔断矩阵 | `social/permissions/__init__.py` | 空文件，无安全机制 |
| 🔴 P0 | API 路由层 | `api/routes/chat.py` 等 | 桩代码，前后端断连 |
| 🟡 P1 | 前端 UI | `web/src/app/` | chat/memory/skills 目录不存在 |
| 🟡 P1 | 宪法权限测试 | `social/constitution/core.py` | 代码 169 行但测试为零 |

---

## Part 1：后端模块验收标准

### Layer 1：LLM 大脑层（完成度 80%）

#### 1.1 多模型路由器 ✅

| 条件 | 类型 | 验收标准 | 测试命令/方法 |
|------|------|---------|--------------|
| 模型列表 API | `[A]` | `GET /api/v1/models` 返回列表，每条含 `model_id`、`provider`、`latency_ms`，至少1条可用 | `pytest tests/cortex/test_llm_router.py` |
| 故障转移 | `[A]` | 主模型连续失败 3 次（mock），第4次路由到回退模型，日志记录切换原因 | `pytest tests/cortex/test_llm_router.py::test_failover` |
| 退避时间 | `[A]` | 第1次重试 2s，第2次 4s，第3次 8s，时间戳差值允许误差 ±200ms | mock sleep + 时间戳验证 |
| 并发路由 | `[A]` | 并发 10 个请求，响应 `request_id` 与请求一一对应，无丢失 | asyncio 并发测试 |

#### 1.2 心绪感知 Prompt ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 状态差异性 | `[A]` | `calm`/`focused`/`anxious`/`crisis` 四种 system prompt，任意两种 Jaccard 相似度 < 0.5 |
| Crisis 简化 | `[A]` | `crisis` 状态 prompt 字数 ≤ `calm` 状态的 50%（防止超长 prompt 拖慢响应）|
| 温度联动 | `[A]` | `calm` 状态 temperature < `crisis` 状态 temperature（日志中 API 请求参数可验证）|

#### 1.3 Token 预算管理 ⚠️

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| Crisis 压缩 | `[A]` | `crisis` 模式下 `max_tokens` ≤ 正常模式的 60%（精确数值对比）|
| 多核预算 | `[A]` | 4 核模式下各核 `max_tokens` 之和 ≤ 配置的总预算上限 |

---

### Layer 2：Agent 核心引擎（完成度 43%）⚠️

#### 2.1 Trinity 多核治理引擎 ✅

| 条件 | 类型 | 验收标准 | 当前状态 |
|------|------|---------|---------|
| 1 核直通 | `[A]` | 1 核模式：Core-0 直接返回，日志无 Core-1/2/3 记录，框架延迟 < 2s | ✅ |
| 2 核串行 | `[M]` | Core-0 生成 → Core-1 审计 → 综合，UI 显示两个独立输出面板+角色标签 | ⚠️ API桩 |
| 3 核并行 | `[A]` | Core-1 与 Core-2 启动时间戳差 < 200ms，输出不互相等待 | ✅ |
| 冲突仲裁 | `[A]` | Core-1 与 Core-2 矛盾时，系统按预设策略（多数投票）输出定稿，日志记录冲突 | ✅ |
| 升核触发 | `[A]` | L2 操作自动升至 ≥3 核，L3 强制 4 核且等待人工确认信号 | ⚠️ 宪法测试缺失 |

#### 2.2 心绪引擎 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 状态范围 | `[A]` | `entropy`、`valence` 均 ∈ [0.0, 1.0]，边界：10次失败后 entropy ≤ 1.0，不溢出 |
| 事件响应 | `[A]` | `llm_call_success` 触发后 entropy 减少 ∈ [0.015, 0.025]；`task_failed` 触发后增加 ∈ [0.07, 0.09] |
| 危机跃迁 | `[A]` | entropy ≥ 0.80 时 state 变为 `"crisis"`，UI 心绪指示灯 CSS class 变为 `status-red` |
| 持久化 | `[A]` | 服务重启后 entropy 误差 < 0.01（从 JSON 文件恢复）|
| 测试命令 | — | `pytest tests/cortex/test_heart.py -v` |

#### 2.3 ReAct 执行循环 🔲 ← **P0 最高优先级**

> **当前状态：空文件。整个 Agent 执行引擎核心，不实现则 MVP 无法发布。**

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| Plan 生成 | `[A]` | 多步任务返回 JSON Plan 数组，每项含 `step_id`、`action`、`expected_observation` |
| 步骤日志 | `[A]` | 每步感知/思考/行动/观察均有日志行，格式 `[REACT][step_N][phase] message` |
| 失败恢复 | `[A]` | 中间步骤出错时，生成新 Plan 继续，不抛出异常中止 |
| 无限循环防护 | `[A]` | 超过 20 步（可配置）后强制终止，返回 `{"status": "max_steps_exceeded", "steps_taken": N}` |
| 并发隔离 | `[A]` | 同时 3 个多步任务，各任务日志 `session_id` 不交叉混淆 |

**实现建议**：约 200-300 行 Python，参考 `cortex/trinity/engine.py` 的纯函数风格

#### 2.4 上下文压缩引擎 ⚠️

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 自动触发 | `[A]` | token 用量超上限 70% 时触发，日志记录压缩事件 |
| 压缩率 | `[A]` | 压缩后字符数 ≤ 原始字符数的 40%（减少 ≥ 60%）|
| 关键内容保留 | `[M]` | 压缩后代码块有 hash 可匹配版本，`Error:` 关键词至少保留一处 |
| 写入温记忆 | `[A]` | 压缩完成后 L1 数据库可通过会话 ID 查到压缩摘要条目 |

---

### Layer 3：永远记忆系统（完成度 73%）

#### 3.1 四层记忆架构 ✅

| 层 | 条件 | 类型 | 验收标准 |
|----|------|------|---------|
| L0 | 读写一致 | `[A]` | 写入 10 条消息立即读取，返回相同 10 条，顺序一致 |
| L0→L1 | 会话结束转存 | `[A]` | 调用 `end_session` 后，L1 中可查到该会话摘要条目 |
| L1 | 语义召回 | `[A]` | 写入"用 Python 开发后端"，查"项目编程语言"，top-1 相似度 ≥ 0.75 |
| L1 | 时间衰退 | `[A]` | 同语义内容，30天前写入的记忆检索分 ≤ 新记忆分的 60% |
| L1 | 降级保护 | `[A]` | 向量库不可用时，内容写入本地临时文件，恢复后自动同步 |
| L2 | 永久存储 | `[A]` | L1 蒸馏后，L2 中存在完整原文；DELETE 返回 HTTP 403 |
| L3 | DNA 注入 | `[A]` | 写入 DNA 条目后，下次对话 system prompt 包含该条目原文 |
| 测试命令 | — | — | `pytest tests/memory/test_memory.py -v` |

#### 3.2 三阶段梦境蒸馏 ✅

| 阶段 | 条件 | 类型 | 验收标准 |
|------|------|------|---------|
| Phase 1 | 触发 | `[A]` | `POST /api/v1/memory/distill/light` 返回处理条目数，每条标签 ≤ 8 个，无重复 |
| Phase 1 | 幂等性 | `[A]` | 连续触发两次，第二次标签集合与第一次完全相同 |
| Phase 2 | 关联报告 | `[A]` | 返回 `[{"source_id", "target_id", "relation_type", "confidence"}]`，confidence ≥ 0.6 |
| Phase 3 | 梦境日记 | `[M]` | 生成文本含"关键发现"、"新建连接"、"矛盾检测"三章 |
| 调度 | 定时触发 | `[A]` | Phase 1 日志每 2h 出现一次，误差 ≤ 5 分钟 |
| 测试命令 | — | — | `pytest tests/memory/test_distill.py -v` |

#### 3.3 混合检索系统 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 语义检索 | `[A]` | 无关键词重叠的语义查询（"项目编程语言" 查 "Python后端"），top-1 命中 |
| BM25 精确 | `[A]` | 输入 "openagi415"，结果必须包含该精确字符串的记录 |
| MMR 去重 | `[A]` | 5 条结果中任意两条语义相似度 < 0.9（embedding 余弦验证）|
| 空结果处理 | `[A]` | 完全不相关查询返回 `{"results": [], "count": 0}`，不报错 |

---

### Layer 4：永生与分布式层（完成度 40%）

#### 4.1 心跳调度器 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| Calm 心跳 | `[A]` | 连续 5 次心跳间隔均在 290s～310s（日志时间戳验证）|
| Crisis 加速 | `[A]` | 从 calm 切换到 crisis，下次心跳间隔 ≤ 65s |
| 日志内容 | `[A]` | 每次心跳日志含：状态快照写入 ✅、健康指标 ✅、缓存清理条目数 |
| 测试命令 | — | `pytest tests/ghost/test_heartbeat.py -v` |

#### 4.2 状态持久化 ⚠️

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 崩溃恢复 | `[A]` | `kill -9` 后 60s 内进程重新出现在 `ps` 列表，连续 3 次均通过 |
| 熵值恢复 | `[A]` | 重启后 `GET /api/v1/heart/status` 的 entropy 与重启前误差 < 0.01 |
| SQLite 并发 | `[A]` | 并发 10 个写操作不报 `SQLITE_BUSY`（WAL 模式）|

#### 4.3 Docker 部署 ⚠️

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 启动时间 | `[A]` | `docker compose up` 后 `/health` 在 30s 内首次返回 200 |
| 数据持久化 | `[M]` | `docker compose down && up`（保留 volume），记忆数据完整恢复 |
| 验证命令 | — | `docker compose up -d && sleep 30 && curl localhost:8888/health` |

---

### Layer 5：工具与扩展层（完成度 69%）

#### 5.1 工具注册表 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 注册/注销 | `[A]` | `register()` 后 `get_available_tools()` 包含；`unregister()` 后调用返回 HTTP 404 |
| 权限过滤 | `[A]` | `get_available_tools(permission_level="L1")` 不含任何 L2+ 权限工具名称 |

#### 5.2 Hook 系统 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 写文件拦截 | `[A]` | `before_file_write` hook 拒绝时，目标文件不创建，原因记入日志 |
| 敏感信息检测 | `[A]` | 文件含 `ANTHROPIC_API_KEY=sk-`、`ghp_`、`-----BEGIN RSA` 任一，推送被阻止，报告含行号 |
| 异常回滚 | `[A]` | hook 抛出异常时，原操作回滚，日志记录异常类型和堆栈摘要 |
| 测试命令 | — | `pytest tests/tools/test_hooks_builtin.py tests/tools/test_hooks_manager.py -v` |

#### 5.3 MCP 客户端 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 工具发现 | `[A]` | 连接 MCP Server 后，`list_tools()` 返回工具列表 ≥ 1 条 |
| 断连处理 | `[A]` | MCP Server 断开时返回 HTTP 503，不挂起主进程 |
| 多 Server | `[A]` | 同时连接多个 MCP Server，`list_tools()` 按 Server 分组，不混淆 |

#### 5.4 浏览器引擎 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 基础搜索 | `[A]` | Google/百度/GitHub 各搜索一次，均返回 ≥ 5 条结果，每条含 `title`（非空）和 `url`（合法格式）|
| 降级处理 | `[A]` | Chrome 不可用时返回 `{"error": "timeout", "platform": "...", "query": "..."}`，不挂起 |
| 记忆联动 | `[A]` | 同 query 第二次请求，`source` 字段返回 `"memory"`，网络日志无新 CDP 调用 |
| L0 权限隔离 | `[A]` | L0 搜索日志无点击、表单提交、Cookie 写入操作 |

---

### 巡检 AI（完成度 88%）✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 定时精度 | `[A]` | 连续 5 次触发间隔均在 270s～330s（允许 ±30s）|
| 智能等待 | `[A]` | 有任务执行时，巡检时间戳晚于任务完成时间戳 |
| 危机优先 | `[A]` | entropy ≥ 0.80 后，5s 内触发危机巡检，不等待当前任务 |
| 报告结构 | `[A]` | `task:completed` 后，报告 JSON 含 `completed_items`（数组 ≥1）和 `next_steps`（数组 ≥1）|
| 草稿模式 | `[M]` | 巡检内容出现在发送框，用户修改后发送才执行 |
| 测试命令 | — | `pytest tests/cortex/test_commander.py -v` |

---

### 人格系统（完成度 88%）✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 专家库数量 | `[A]` | `GET /api/v1/personas?domain=工程` 返回 ≥ 44 条，每条含 `name`/`domain`/`prompt`/`temperature` |
| 人格差异性 | `[A]` | 任意两种预设人格的 system prompt Jaccard 相似度 < 0.5（不同模式需有本质差别）|
| 自定义持久化 | `[A]` | 自定义人格保存后，重启服务仍存在（JSON 文件持久化）|
| 盲测（非必须）| `[M]` | ≥5 人测评，能以 ≥ 70% 准确率识别当前人格模式 |
| 测试命令 | — | `pytest tests/social/test_persona.py -v` |

---

### 聊天系统（完成度 83%）

#### 深度聊天 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 流式首 token | `[A]` | 发消息后，前端首个 token 到达 < 3s（不含 LLM 首 token 时间）|
| Markdown 渲染 | `[M]` | `**粗体**`、`# 标题`、代码块正确渲染，代码块有语法高亮和复制按钮 |
| 停止响应 | `[A]` | 点击停止后 500ms 内 WebSocket 不再收到新 token |
| Token 标签 | `[A]` | 每条 AI 回复显示 token 数，与后端 `usage.completion_tokens` 一致 |
| 历史持久化 | `[A]` | 刷新页面，历史消息重载，顺序不变 |
| 断线提示 | `[A]` | 流式输出中断网，5s 内显示"连接中断"，不静默失败 |

#### AI 团队群聊 ✅

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 单点 @ | `[A]` | `@成员A` 只有成员 A 产生回复，其他成员日志无回复记录 |
| @全体 | `[A]` | 所有成员按加入顺序依次回复，日志顺序可验证 |
| 防循环 | `[A]` | AI 互@最多 3 轮（用户→A→B→停止），第 4 轮不触发 |
| 讨论↔工作 | `[M]` | 工作模式下出现任务面板，任务分配给指定成员后状态更新为"完成" |

---

### 宪法与权限系统（完成度 25%）⚠️

#### 宪法核心 ⚠️

| 条件 | 类型 | 验收标准 | 当前状态 |
|------|------|---------|---------|
| L0 直通 | `[A]` | L0 操作直接执行，响应时间内无"确认等待"状态 | ⚠️ 无测试 |
| L2 确认 | `[A]` | L2 操作触发确认，用户拒绝后目标文件不变，日志状态为 `"rejected_by_user"` | ⚠️ 无测试 |
| L3 双签 | `[A]` | AI 审计通过但用户未确认 → 不执行；用户确认但 AI 拒绝 → 不执行；两者必须同时通过 | 🔲 |
| L4 永久禁止 | `[A]` | 含"银行密码"等关键词的操作返回 HTTP 403，body 含 `"permanently_forbidden"` | ⚠️ 无测试 |

#### 权限熔断矩阵 🔲 ← **P0 第二优先级**

> **当前状态：空目录。无此模块则无法保证安全。**

| 条件 | 类型 | 验收标准 |
|------|------|---------|
| 越权拦截 | `[A]` | L0 Agent 尝试 L2 操作（写文件），返回 HTTP 403，操作不执行 |
| 权限撤销 | `[A]` | 撤销 L2 授权后，在途的 L2 任务在 3s 内收到取消信号，操作回滚 |
| 宪法权重 | `[A]` | `GET /api/v1/constitution/weights` 返回五维权重，之和 = 1.0 |

**实现建议**：移植自 `NewClaw v6/fuse-matrix.ts`（258 行），约 200 行 Python

---

### 数字伴侣系统（完成度 70%）⚠️

| 条件 | 类型 | 验收标准 | 当前状态 |
|------|------|---------|---------|
| TTS 生成 | `[A]` | 输入 100 字文本，输出 WAV 可用 `ffprobe` 验证有效，生成延迟 < 3s | ✅ Mock |
| STT 转写 | `[A]` | 标准句"你好，我是OpenAGI"，字符准确率 ≥ 85%，延迟 < 5s | ✅ Mock |
| 情绪音调 | `[A]` | `crisis` 状态 TTS `speed` 参数 > `calm` 状态（日志可验证）| ⚠️ |
| 无设备降级 | `[A]` | 无麦克风返回 `{"error": "no_microphone"}` 而非崩溃；无扬声器时 TTS 仍生成文件 | ✅ |
| 亲密度差异 | `[A]` | "伴侣"模式 prompt 含"温馨"/"体贴"，"专业"模式含"客观"/"专业"，各有独有关键词 | ✅ |

---

### API 路由层（完成度 57%）⚠️ ← **P0 第三优先级**

> **当前状态：三个路由文件均为桩代码，返回空列表/占位符。**

| 端点 | 条件 | 类型 | 验收标准 |
|------|------|------|---------|
| `POST /api/v1/chat/send` | 端到端 | `[A]` | 发消息返回真实 AI 回复（非占位符），含 `reply`、`tokens`、`duration_ms` 字段 |
| `GET /api/v1/chat/history` | 历史 | `[A]` | 返回当前会话消息列表，顺序与写入顺序一致 |
| `GET /api/v1/models` | 模型列表 | `[A]` | 返回 ≥ 1 条可用模型，每条含 `model_id`、`provider`、`available: true` |
| `GET /api/v1/memory/search` | 记忆搜索 | `[A]` | 跨 L0/L1/L2 层搜索，返回 `{"layer": "L1_recent", "content": "...", "score": float}` 格式 |
| `GET /health` | 健康检查 | `[A]` | 响应 < 100ms，返回 `{"status": "ok", "version": "x.x.x"}` |
| WS `/ws/chat` | 实时聊天 | `[A]` | 同时建立 5 个 WS 连接，各连接发消息均独立收到回复，互不干扰 |

**验证命令**：
```bash
source .venv/bin/activate
uvicorn openagi.api.main:app --port 8888 &
sleep 3
curl -s http://localhost:8888/health | python -m json.tool
```

---

## Part 2：前端 UI 验收标准（完成度 0%）🔲

> **注意**：`web/src/app/chat/`、`memory/`、`skills/` 目录不存在（比 🔲 更差）

| 页面 / 组件 | 优先级 | `[A]`/`[M]` | 最低验收标准 |
|------------|--------|------------|-------------|
| 聊天页 `app/chat/` | **P0** | `[M]` | 可输入消息并收到 AI 回复，流式显示 |
| 发送框 | **P0** | `[A]` | Enter 发送，Shift+Enter 换行；高度随输入自适应，超 15 行出现内部滚动条 |
| 心绪指示灯 | **P0** | `[A]` | CSS class 与 API `state` 值一致：`calm=status-green`，`crisis=status-red` |
| 侧边栏 | P1 | `[M]` | 新建会话、点击切换会话；点击收起按钮后主区域宽度增加 |
| 首页欢迎屏 | P1 | `[A]` | 按本地时区显示问候语（6-12时 `早上好☀️`，12-18时 `下午好`，18-24时 `晚上好🌙`）|
| 多核治理面板 | P1 | `[M]` | 1/2/3/4 核切换有效，切换后下次发送使用新核数 |
| 群聊页 `app/group/` | P1 | `[M]` | 可添加成员，发送 @ 消息触发对应成员回复 |
| 记忆宫殿页 `app/memory/` | P1 | `[M]` | 显示 L0/L1/L2/L3 四层统计数字，L3 DNA 可增删改 |
| 设置页 `app/settings/` | P2 | `[M]` | 22 个设置页均可渲染；修改后刷新仍生效 |
| Token 用量 | P1 | `[A]` | 刷新后从 `GET /api/v1/usage/today` 重新获取，不归零 |
| 断线指示 | P1 | `[A]` | `kill -9` 后端后，5s 内前端连接指示变为断线状态 |

---

## Part 3：集成链路验收（五条端到端路径）

| 链路 | 经过模块 | 类型 | 验收标准 | 当前状态 |
|------|---------|------|---------|---------|
| **① 消息→回复** | SendBox → chat/deep → trinity → llm/router → 回显 | `[A]` | 用户消息返回 AI 回复，含 `reply`/`audit`/`tokens` 字段 | 🔲 loop缺失 |
| **② 心绪→LLM** | heart/entropy → llm/router（温度注入）| `[A]` | calm→crisis 切换后，下次 LLM 请求 `temperature` 升高，`max_tokens` 降低 | ⚠️ |
| **③ 记忆全链** | chat → memory/manager → distill → search → prompt注入 | `[A]` | 新对话提及上次主题，AI 回复包含正确历史内容 | ⚠️ |
| **④ 权限熔断** | 任意操作 → constitution/core → L0-L4判断 | `[A]` | L0 操作直通；L4 操作返回 HTTP 403 永久拒绝 | 🔲 permissions空 |
| **⑤ 心跳持久化** | ghost/heartbeat → light_sleep → archive → 重启恢复 | `[A]` | kill -9 后 60s 内重启，记忆和心绪状态完整恢复 | ⚠️ |

---

## Part 4：MVP Go/No-Go 检查单

### 🔴 必须全部 ✅（发布硬性门槛）

- [ ] `cortex/loop/` ReAct 循环实现（最大阻塞项）
- [ ] `social/permissions/` 权限熔断矩阵实现
- [ ] API 路由三文件（chat/settings/skills）接通真实逻辑
- [ ] `python -m pytest tests/ -v` 全部通过，无 ERROR，无 SKIP
- [ ] 集成链路 ① 消息→回复 端到端通过
- [ ] 集成链路 ④ 权限熔断 通过
- [ ] `docker compose up && curl /health` 返回 200

### 🟡 可以 ⚠️（发布后迭代）

- [ ] 前端 UI：至少聊天页可收发消息
- [ ] 记忆蒸馏：Light Sleep 可调度，REM/Deep 可 Mock
- [ ] TTS/STT：Mock 实现通过即可
- [ ] Telegram Bot：有 Token 时可用即可

### 🟢 不阻塞 MVP

- [ ] `social/economy/` 经济系统（Phase 2）
- [ ] `memory/governance/` 记忆治理（Phase 2）
- [ ] Redis 跨节点同步（Phase 2）
- [ ] AI 自拍系统（Phase 2）
- [ ] 技能市场 HTTP 源联网（Phase 2）

---

## Part 5：当前完成度总览

| 分类 | 子项数 | ✅ | ⚠️ | 🔲 | 完成度 |
|------|--------|----|----|-----|--------|
| Layer 1 LLM大脑层 | 5 | 3 | 2 | 0 | 80% |
| Layer 2 核心引擎 | 5 | 3 | 1 | 1 | 43% |
| Layer 3 记忆系统 | 11 | 7 | 3 | 1 | 73% |
| Layer 4 永生层 | 5 | 1 | 3 | 1 | 40% |
| Layer 5 工具层 | 8 | 5 | 2 | 1 | 69% |
| 巡检AI | 6 | 5 | 1 | 0 | 88% |
| 人格系统 | 4 | 3 | 1 | 0 | 88% |
| 聊天系统 | 9 | 6 | 3 | 0 | 83% |
| 宪法权限 | 7 | 0 | 3 | 4 | 21% |
| 数字伴侣 | 5 | 3 | 2 | 0 | 70% |
| API路由层 | 6 | 2 | 4 | 0 | 50% |
| **前端 UI** | **11** | **0** | **0** | **11** | **0%** |
| **合计** | **82** | **38** | **25** | **19** | **约 63%** |

---

## v2 相对 v1 的改进

| 改进点 | v1 做法 | v2 做法 |
|--------|--------|--------|
| 验收标准精度 | ✅/⚠️ 模糊状态 | 量化数字（时间/比率/相似度/错误码）|
| 可测试性 | 无 AUTO/MANUAL 区分 | `[A]`/`[M]` 标签，AUTO 占 ~70% |
| 快速定位 | 需阅读全文 | 开头"当前最大缺口"表格，一目了然 |
| 实现建议 | 无 | P0 模块附"实现建议"（行数估计/参考文件）|
| 集成测试 | 仅链路名 | 每条有具体断言和当前状态 |
| 测试命令 | 通用命令 | 每模块附对应 pytest 命令 |

---

*验收总命令（全量）：*
```bash
source .venv/bin/activate
python -m pytest tests/ -v --tb=short 2>&1 | tail -30
```
*M2 请注意：ChromaDB 首次初始化需下载 ~1GB 嵌入模型，请在稳定网络下运行*
