# OpenAGI 全站骨架树（去叶留枝版）

> 15 个页面 · 生成日期 2026-04-12 · 点击展开各页面详情

---

## 目录（TOC）

1. [概览 /overview](#1-概览-overview)
2. [模型 /models](#2-模型-models)
3. [聊天 /chat](#3-聊天-chat)
4. [智能体 /agents](#4-智能体-agents)
5. [消息频道 /channels](#5-消息频道-channels)
6. [技能 /skills](#6-技能-skills)
7. [定时任务 /cron](#7-定时任务-cron)
8. [设置 /settings](#8-设置-settings)
9. [初始化向导 /setup](#9-初始化向导-setup)
10. [实例管理 /instances](#10-实例管理-instances)
11. [会话管理 /sessions](#11-会话管理-sessions)
12. [文档 /documents](#12-文档-documents)
13. [使用情况 /usage](#13-使用情况-usage)
14. [Trinity /trinity](#14-trinity-trinity)
15. [经济 /trinity（Economy 嵌套）](#15-经济-trinity-economy-嵌套)

---

## 全站统计表

| # | 页面名 | 路由 | 复杂度星级 | Toast 数量 | 弹窗数量 |
|---|--------|------|-----------|-----------|---------|
| 1 | 概览 | /overview | ★☆☆ | 0 | 0 |
| 2 | 模型 | /models | ★★★ | 12 | 2（确认删除 + 用量详情） |
| 3 | 聊天 | /chat | ★★★ | 0（内嵌 Trinity 面板） | 1（删除会话确认） |
| 4 | 智能体 | /agents | ★★☆ | 14 | 4（添加/删除/设置/模型覆盖/未保存确认） |
| 5 | 消息频道 | /channels | ★★★ | 11 | 2（配置弹窗 + 删除确认） |
| 6 | 技能 | /skills | ★★☆ | 16 | 0（使用 Sheet 面板） |
| 7 | 定时任务 | /cron | ★★☆ | 13 | 2（创建/编辑弹窗 + 删除确认） |
| 8 | 设置 | /settings | ★★★ | 20 | 0（展开面板，无独立弹窗） |
| 9 | 初始化向导 | /setup | ★★☆ | 8 | 0（步骤内嵌流程） |
| 10 | 实例管理 | /instances | ★☆☆ | 0 | 0 |
| 11 | 会话管理 | /sessions | ★☆☆ | 2 | 1（终止确认） |
| 12 | 文档 | /documents | ★☆☆ | 0 | 0 |
| 13 | 使用情况 | /usage | ★☆☆ | 0 | 0 |
| 14 | Trinity | /trinity | ★★★★ | 28 | 多个（目标放弃/联邦清算/分红/购买等） |
| 15 | 经济（嵌套于 Trinity） | /trinity/blockchain 等 | ★★☆ | 13 | 3（区块链/分红/购买确认） |

---

<details>
<summary><h2>1. 概览 /overview ★☆☆</h2></summary>

# Overview 页面 — 骨架树（去叶留枝版）

```
/overview
│
├── 页面头部
│   ├── 标题："概览"
│   ├── 副标题："系统运行状态一览"
│   └── [刷新] 按钮（初始加载 + 手动刷新时图标旋转）→ 重新拉取所有数据
│
├── 统计卡片区（四宫格）
│   │
│   ├── 网关状态卡片
│   │   ├── 标签："网关状态"
│   │   ├── 值："运行中"（state=running）/ "已停止"（state≠running）
│   │   └── 副信息："端口 {port}"（默认 18799）
│   │
│   ├── 活跃会话卡片
│   │   ├── 标签："活跃会话"
│   │   ├── 值：活跃会话数（数字）
│   │   └── 副信息："共 N 个会话"
│   │
│   ├── 消息渠道卡片
│   │   ├── 标签："消息渠道"
│   │   ├── 值："N/M"（健康数/总数）
│   │   └── 副信息："全部正常"（全部健康时）/ "N 个异常"（有异常时）
│   │
│   └── 智能体卡片
│       ├── 标签："智能体"
│       ├── 值：运行中的智能体数量
│       └── 副信息："N 个已配置"
│
├── 渠道健康状态区
│   ├── 区域标题："渠道健康状态"
│   │
│   ├── 有渠道时 → 渠道卡片列表（网格）
│   │   └── 每张卡片
│   │       ├── 状态图标：✓（健康）/ ✗（异常）
│   │       ├── 渠道名称："{type}"（有 accountName 时追加 " · {accountName}"）
│   │       ├── 状态描述："正常运行" / "连接异常"
│   │       ├── 最后成功时间："最后成功: {zh-CN 本地化时间}"（如有）
│   │       └── 状态徽章："健康" / "异常"
│   │
│   └── 无渠道时 → 空状态
│       ├── 警告图标（AlertTriangle）
│       ├── "暂无已配置的渠道"
│       └── "前往「消息渠道」页面添加 WhatsApp、Telegram 等"
│
└── 智能体状态区
    ├── 区域标题："智能体状态"
    │
    ├── 有智能体时 → 智能体卡片列表（网格）
    │   └── 每张卡片
    │       ├── 智能体名称：{name}（无名时显示 {id}）
    │       └── 描述：{description}（无描述时显示 "AI 智能体"）
    │
    └── 无智能体时 → 空状态
        ├── Bot 图标
        └── "暂无智能体"
```

## 数据流骨架

```
页面加载
  → 同时拉取两个 API：
  │   ├── GET /api/gateway/channels → 获取渠道列表及健康状态
  │   └── GET /api/gateway/sessions/stats → 获取活跃/总会话数
  → 任一 API 失败时静默处理（不报错、不 Toast）
  → 每 15 秒自动刷新
  → 手动点击 [刷新] 立即刷新

网关状态
  → 来自 useGatewayStore（全局 store）
  → 字段：state（running/stopped）、port（默认 18799）

智能体列表
  → 来自 useAgentsStore（全局 store）
  → 计算运行中数量：agent.running !== false 的数量
```

## 特殊说明

- 本页面无 Toast 消息（API 错误静默处理）
- 本页面无弹窗/对话框
- 本页面无编辑操作，纯展示型
- 统计卡片的 StatCard 是页面内部组件，非全局组件

</details>

---

<details>
<summary><h2>2. 模型 /models ★★★</h2></summary>

# Models 页面 — 骨架树（去叶留枝版）

```
/models
│
├── 页面头部
│   ├── 标题："模型"
│   └── 副标题："管理您的 AI 提供商并监控 Token 用量"
│
├── 提供商管理区
│   ├── 区域标题："AI 模型提供商" + [添加提供商] 按钮
│   │
│   ├── 空状态（无提供商时）
│   │   ├── 标题："未配置提供商"
│   │   ├── 描述："添加 AI 提供商以开始使用 OpenAGI"
│   │   └── 按钮："添加您的第一个提供商"
│   │
│   ├── 提供商卡片列表（有提供商时）
│   │   └── 每张卡片
│   │       ├── 查看模式
│   │       │   ├── 提供商图标 + 名称
│   │       │   ├── 认证模式："API 密钥" / "OAuth 设备登录" / "OAuth 浏览器登录" / "本地"
│   │       │   ├── 状态标识："已配置"（绿点）/ "未设置 API 密钥"（红点）
│   │       │   ├── 默认标识："默认" 徽章（如果是默认）
│   │       │   ├── 回退信息："回退：提供商A, 提供商B"（如有）
│   │       │   └── 操作按钮（悬停显示）
│   │       │       ├── [设为默认]（非默认时）
│   │       │       ├── [编辑]
│   │       │       └── [删除] → 弹出确认对话框
│   │       │
│   │       └── 编辑模式（点击编辑后展开）
│   │           ├── 基础 URL 输入框 ※见字段映射表
│   │           ├── 模型 ID 输入框 ※见字段映射表
│   │           ├── 协议选择 ※见字段映射表
│   │           │   ├── [OpenAI Completions]
│   │           │   ├── [OpenAI Responses]
│   │           │   └── [Anthropic 兼容]
│   │           ├── User-Agent 输入框 ※见字段映射表
│   │           ├── 回退配置（可折叠）
│   │           │   ├── 同 Provider 回退模型 ID（多行文本框）
│   │           │   └── 跨 Provider 回退（复选框列表）
│   │           │       └── 空状态："请先添加其他 provider，才能把它设为回退目标。"
│   │           ├── API 密钥输入框 + 显示/隐藏切换
│   │           │   ├── 已有 key："这个 provider 已经保存了 API key。"
│   │           │   ├── 无 key："这个 provider 还没有保存 API key。"
│   │           │   ├── 替换提示："如果想保留当前已保存的 API key，这里留空即可。"
│   │           │   └── 安全提示："您的 API 密钥存储在本地机器上。"
│   │           ├── Ark Code Plan 预设切换（仅 ByteDance Ark）
│   │           │   ├── [API Key] / [Code Plan] 模式切换
│   │           │   └── 说明文字 + 文档链接
│   │           └── [保存]（保存中显示加载动画+禁用） / [取消] 按钮
│   │
│   └── 添加提供商对话框（点击"添加提供商"后弹出）
│       ├── 头部："添加 AI 提供商" + 描述 + [关闭]
│       │
│       ├── 第一步：选择提供商类型（网格）
│       │   ├── Anthropic
│       │   ├── OpenAI（支持 OAuth）
│       │   ├── Google（支持 OAuth）
│       │   ├── OpenRouter
│       │   ├── MiniMax 国内站（支持 OAuth）
│       │   ├── Moonshot
│       │   ├── SiliconFlow
│       │   ├── MiniMax 国际站（支持 OAuth，与国内站互斥）
│       │   ├── Model Studio（仅开发者模式可见）
│       │   ├── ByteDance Ark
│       │   ├── Ollama（本地，无需 API Key，自动填充占位密钥）
│       │   └── Custom（自定义，支持多账户）
│       │   ※ 已存在且不支持多账户的提供商显示为禁用状态
│       │
│       └── 第二步：配置表单（选择类型后）
│           ├── [更换提供商] / [查看文档] 链接
│           ├── 显示名称输入框
│           │
│           ├── 认证方式切换（支持 OAuth 的提供商）
│           │   ├── [OAuth 登录] tab
│           │   │   └── OAuth 流程
│           │   │       ├── 初始："此提供商需要通过浏览器登录授权。"
│           │   │       │   ├── [浏览器登录] 按钮
│           │   │       │   └── [获取 API 密钥] 链接（跳转到提供商官网）
│           │   │       ├── 等待："正在获取安全登录码..."
│           │   │       ├── 设备码显示
│           │   │       │   ├── "确认登录"
│           │   │       │   ├── 步骤1："复制下方的授权码。"
│           │   │       │   ├── 步骤2："在浏览器中打开登录页面。"
│           │   │       │   ├── 步骤3："粘贴授权码以批准访问。"
│           │   │       │   ├── 授权码 + [复制] → Toast "代码已复制到剪贴板"
│           │   │       │   └── "等待浏览器中的授权..."
│           │   │       ├── 手动输入码（备用：粘贴授权码 + 提交）
│           │   │       ├── 失败："认证失败" + [重试]
│           │   │       │   └── 浏览器流程不可用时："该提供商的浏览器 OAuth 登录链路暂未接通。"
│           │   │       └── 成功 → 自动关闭 + Toast "提供商添加成功"
│           │   │
│           │   └── [API 密钥] tab
│           │       └── （同下方 API Key 流程）
│           │
│           ├── API Key 认证流程（不支持 OAuth 的提供商直接进入）
│           │   ├── 基础 URL ※见字段映射表
│           │   ├── 模型 ID ※见字段映射表
│           │   ├── 协议选择 ※见字段映射表
│           │   ├── User-Agent ※见字段映射表
│           │   ├── API 密钥 + 显示/隐藏
│           │   └── Ark 模式切换（Ark 专属）
│           │
│           └── [添加提供商] 按钮（保存中显示加载动画+禁用）
│
├── Token 用量历史区
│   ├── 标题："最近 Token 消耗"
│   │
│   ├── 控制按钮组
│   │   ├── 分组切换：[按模型] / [按时间]
│   │   └── 时间窗口：[7 天] / [30 天] / [全部]
│   │
│   ├── 柱状图（水平堆叠条）
│   │   ├── 图例：● 输入  ● 输出  ● 缓存
│   │   └── 每个分组一行：标签 + 彩色柱体 + 总数
│   │       └── 按模型最多显示 8 组
│   │
│   ├── 记录统计："共 N 条记录"
│   │
│   ├── 用量条目列表（每页 5 条）
│   │   └── 每条记录
│   │       ├── 模型名称（或"未知模型"）
│   │       ├── 总 token 数
│   │       │   ├── 正常：显示数字
│   │       │   ├── 缺失（usageStatus=missing）：显示 "?"
│   │       │   └── 错误（usageStatus=error）：显示 "!"
│   │       ├── 时间戳
│   │       ├── 明细：输入 N · 输出 N · 缓存读取 N · 缓存写入 N
│   │       ├── 费用："费用 $0.0123"（如有）
│   │       └── [查看内容]（仅开发者模式 + 有内容时）
│   │
│   ├── 分页控件
│   │   ├── [上一页]（第1页禁用）
│   │   ├── "第 1 / 3 页"
│   │   └── [下一页]（最后一页禁用）
│   │
│   ├── 用量详情弹窗（开发者模式点击条目）
│   │   ├── 标题："用量明细内容"
│   │   ├── 模型名 + 时间戳
│   │   ├── 原始 JSON 内容（等宽字体）
│   │   └── [关闭] 按钮 + Escape 键关闭
│   │
│   └── 特殊状态
│       ├── 加载中："正在加载 token 消耗历史..."
│       ├── 全局空："还没有 token 消耗历史"
│       ├── 时间窗口空："当前时间范围内没有 token 消耗历史"
│       ├── 网关未运行："无用量数据"
│       └── 解析错误："用量解析失败"
│
└── Toast 消息（操作反馈，共12种）
    ├── "提供商添加成功" / "添加提供商失败"
    ├── "提供商已删除" / "删除提供商失败"
    ├── "默认提供商已更新" / "设置默认失败"
    ├── "提供商已更新" / "更新提供商失败"
    ├── "无效的 API 密钥"
    ├── "需要模型 ID"
    ├── "不能同时添加 MiniMax 国际站和国内站的服务商。"
    └── "代码已复制到剪贴板"
```

## 提供商字段映射表

```
                    基础URL  模型ID  协议选择  User-Agent  需要API Key
Anthropic             -        -        -         -          ✓
OpenAI                -        -        -         -          ✓（或OAuth）
Google                -        -        -         -          ✓（或OAuth）
OpenRouter            -        ✓        -         -          ✓
MiniMax 国内站        -        -        -         -          ✓（或OAuth）
Moonshot              ✓        -        -         -          ✓
SiliconFlow           ✓        -        -         -          ✓
MiniMax 国际站        -        -        -         -          ✓（或OAuth）
Model Studio          ✓        -        -         -          ✓
ByteDance Ark         ✓        ✓        -         -          ✓
Ollama                ✓        -        ✓         -          ✗（自动填充）
Custom                ✓        ✓        ✓         ✓          ✓
```

## 开发者模式说明

- 入口：设置页面 → 通用 → 开发者模式开关
- 影响本页面的功能：
  - Model Studio 提供商在选择网格中可见
  - 部分提供商额外显示模型 ID 字段
  - Token 用量条目显示 [查看内容] 按钮
  - 点击条目可打开用量详情弹窗

## 数据流骨架

```
Token 用量数据
  → 网关运行中时自动拉取
  → 刷新触发：每15秒定时 / 窗口获得焦点 / 页面从后台切回前台
  → 重试：最多2-3次，间隔1.5秒
  → 过滤隐藏来源 → 按时间窗口筛选 → 按维度分组 → 分页显示

提供商操作
  → 添加：选类型 → 填表单 → 验证 API Key → 创建账户 → 刷新列表
  → 编辑：展开表单 → 修改字段 → 验证 → 更新账户 → 刷新列表
  → 删除：确认对话框 → 删除账户 → 刷新列表
  → 设默认：直接设置 → 刷新列表
  → OAuth：发起授权 → 等待设备码 → 浏览器确认 → 回调成功 → 刷新列表
        ↳ 可取消 / 可手动输入码 / 失败可重试
```

</details>

---

<details>
<summary><h2>3. 聊天 /chat ★★★</h2></summary>

# Chat 页面 — 骨架树（去叶留枝版）

```
/chat（路由：/）
│
├── 侧边栏（Sidebar — 跨页面持久存在，Chat 专属功能在此）
│   ├── 顶部区域
│   │   ├── Logo + "OpenAGI" 文字（展开状态）
│   │   └── [折叠/展开侧边栏按钮]（图标切换：PanelLeftClose / PanelLeft）
│   │
│   ├── 导航区
│   │   └── [新对话] 按钮 → 若当前有消息则创建新 session，导航到 /
│   │       （无消息时仅导航，不创建 session）
│   │
│   ├── 会话历史列表（侧边栏展开 + 有 sessions 时显示）
│   │   ├── 按时间分桶排列（降序）
│   │   │   ├── "今天"
│   │   │   ├── "昨天"
│   │   │   ├── "一周内"
│   │   │   ├── "两周内"
│   │   │   ├── "一个月内"
│   │   │   └── "一个月之前"
│   │   │
│   │   └── 每条会话条目
│   │       ├── Agent 标签徽章（显示 agent 名称）
│   │       ├── 会话标签（优先级：sessionLabels > label > displayName > key）
│   │       ├── [点击切换] → switchSession + 导航到 /
│   │       └── [删除图标]（悬停显示）→ 弹出删除确认对话框
│   │           └── 删除确认对话框
│   │               ├── 标题："确认"
│   │               ├── 消息："确定要删除对话 \"{{label}}\" 吗？"
│   │               ├── [删除] 按钮（危险样式）→ deleteSession + 若是当前 session 则导航到 /
│   │               └── [取消] 按钮
│   │
│   └── 底部
│       └── [设置] 导航项 → /settings
│
├── 工具栏区（ChatToolbar）
│   ├── 当前 Agent 标签（仅桌面宽度显示）
│   │   └── "当前对话对象：{{agent}}"
│   │
│   ├── [Trinity 双核博弈按钮]（Shield 图标）→ 切换 Trinity 面板
│   │   └── Trinity 面板（浮层，打开时显示）
│   │       ├── 标题："Trinity 双核博弈" + [关闭按钮]
│   │       │
│   │       ├── 模式选择器（三选一）
│   │       │   ├── [辩论模式]
│   │       │   ├── [竞争模式]
│   │       │   └── [精炼模式]
│   │       │
│   │       ├── [启动 Trinity 循环] → POST /api/trinity/cycle/run { mode }
│   │       │   └── 运行中状态："三体协商中..." + 禁用
│   │       │
│   │       ├── 结果展示（成功时）
│   │       │   ├── AI-1 扩张者 · 提案区
│   │       │   │   ├── 提案标题
│   │       │   │   └── 提案描述（截取前200字符）
│   │       │   │
│   │       │   ├── AI-2 风控员 · 审计区
│   │       │   │   ├── 风险等级徽章（low/medium/high）
│   │       │   │   ├── findings 列表：[严重度] 描述
│   │       │   │   └── "置信度: N% | 通过: 是/否"
│   │       │   │
│   │       │   └── AI-3 财务官 · 决策区
│   │       │       ├── 批准/否决 徽章
│   │       │       ├── 决策理由文本
│   │       │       ├── 优先分数徽章
│   │       │       ├── 执行结果徽章（"已执行" / "已丢弃"）
│   │       │       └── New.B 奖励徽章（奖励 > 0 时显示）："+ N New.B"
│   │       │
│   │       └── 错误状态
│   │           ├── "错误: {{message}}"
│   │           └── "请确认已完成 Genesis 创世并设置 AI 供应商。"
│   │
│   ├── [刷新按钮]（RefreshCw 图标）→ refresh()
│   │   └── Tooltip："刷新聊天"
│   │   └── 加载中状态：图标旋转 + 禁用
│   │
│   └── [思考过程切换按钮]（Brain 图标）→ toggleThinking()
│       ├── Tooltip（showThinking=true）："隐藏思考过程"
│       └── Tooltip（showThinking=false）："显示思考过程"
│
├── 消息区
│   │
│   ├── 空状态（无消息 + 未发送中）→ WelcomeScreen
│   │   ├── 大标题："我能为你做些什么？"
│   │   └── 快捷操作按钮组（无点击行为，仅提示）
│   │       ├── [处理任务]
│   │       ├── [持续执行]
│   │       └── [多智能体并行]
│   │
│   └── 消息列表（有消息时）
│       ├── 历史消息列表（从 chat.history 加载）
│       │   └── 每条消息 → ChatMessage 组件（见消息组件详情）
│       │
│       ├── 流式消息（sending=true + 有流内容时）→ ChatMessage（isStreaming=true）
│       │   └── 含 streamingTools 工具状态栏
│       │
│       ├── 活动指示器（sending=true + pendingFinal=true + 无流内容时）
│       │   └── Sparkles 头像 + Loader2 图标 + "Processing tool results…"
│       │
│       └── 打字指示器（sending=true + pendingFinal=false + 无流内容时）
│           └── Sparkles 头像 + 三点跳动动画
│
├── 错误条（error 不为空时显示）
│   ├── AlertCircle 图标 + 错误文本
│   └── [忽略] 按钮 → clearError()
│
├── 输入区（ChatInput）
│   │
│   ├── 附件预览区（有附件时显示）
│   │   └── 每个附件 → AttachmentPreview
│   │       ├── 图片：缩略图（正方形裁剪）
│   │       ├── 非图片：文件图标 + 文件名 + 文件大小
│   │       │   └── 文件图标映射：视频→Film / 音频→Music / 文本/JSON/XML/PDF→FileText
│   │       │                    压缩包→FileArchive / 其他→File
│   │       ├── 加载中遮罩（staging 状态）：Loader2 旋转
│   │       ├── 错误遮罩（error 状态）："Error"
│   │       └── [删除按钮]（悬停显示 X）→ removeAttachment()
│   │
│   ├── 输入框主体（支持拖放高亮边框）
│   │   ├── 选中 Agent 芯片（targetAgentId 已选时）
│   │   │   ├── 显示："@{{agent}}"
│   │   │   └── [X 按钮] → 清除 targetAgentId
│   │   │
│   │   ├── [附件按钮]（Paperclip 图标）→ pickFiles()（原生文件选择对话框）
│   │   │   └── 禁用条件：disabled 或 sending
│   │   │
│   │   ├── [Agent 选择按钮]（@ 图标，仅有可选 Agent 时显示）→ 切换 Agent 选择器
│   │   │   └── Agent 选择器弹出层
│   │   │       ├── 标题："将下一条消息直接发送给其他 Agent"（显示当前 Agent 名称）
│   │   │       └── Agent 列表（排除当前 Agent）
│   │   │           └── 每个 AgentPickerItem
│   │   │               ├── Agent 名称
│   │   │               ├── 模型显示名称
│   │   │               └── [点击] → 选中 targetAgentId + 关闭选择器 + 聚焦输入框
│   │   │
│   │   ├── 文本输入框（自动扩展，最高 200px）
│   │   │   ├── 占位符（disabled 时）："网关未连接..."
│   │   │   ├── Enter 发送（非 IME 输入法组合状态时）
│   │   │   ├── Shift+Enter 换行
│   │   │   ├── Backspace（无文字 + 有 targetAgentId 时）→ 清除 targetAgentId
│   │   │   ├── 粘贴文件（Ctrl/Cmd+V）→ stageBufferFiles()
│   │   │   └── 拖放文件 → stageBufferFiles()
│   │   │
│   │   └── [发送/停止按钮]
│   │       ├── 发送状态（sending=false）：SendHorizontal 图标
│   │       │   ├── 可发送（有文字或附件 + 附件全部 ready + 未禁用）→ handleSend()
│   │       │   └── 不可发送：半透明禁用样式
│   │       └── 停止状态（sending=true）：Square 图标 → handleStop() → abortRun()
│   │
│   └── 状态栏（输入框下方）
│       ├── 网关状态指示灯（绿色/红色圆点）
│       │   └── "gateway {{state}} | port: {{port}} {{pid}}"
│       │       └── state 已连接时显示："已连接"
│       └── [重试失败的附件]（有失败附件时显示）→ 清除失败附件 + 重新 pickFiles()
│
└── 加载覆盖层（minLoading=true + sending=false 时显示）
    └── 半透明背景 + LoadingSpinner（size="md"）
```

---

## 消息组件详情（ChatMessage）

```
ChatMessage（单条消息）
│
├── 角色判断
│   ├── toolresult 角色 → 不渲染（直接返回 null）
│   └── 空消息（无文字/思考/图片/工具/附件/流式工具状态）→ 不渲染
│
├── 用户消息（role=user，右对齐，蓝色气泡）
│   ├── 图片附件（来自 content blocks，位于文字上方）
│   │   └── ImageThumbnail → 点击放大 → ImageLightbox
│   ├── 文件附件（来自 _attachedFiles）
│   │   ├── 图片类（有 preview）→ ImageThumbnail → 点击放大 → ImageLightbox
│   │   ├── 图片类（无 preview）→ 占位框（File 图标）
│   │   └── 非图片类 → FileCard（可点击打开文件，若有 filePath）
│   ├── 文字气泡（有文字时）
│   │   └── 纯文本显示（whitespace-pre-wrap）
│   └── 时间戳（悬停时显示，相对时间格式）
│       └── 格式：just now / Nm ago / Nh ago / HH:MM
│
└── AI 助手消息（role=assistant，左对齐，带 Sparkles 头像）
    ├── 工具状态栏（isStreaming=true + 有 streamingTools 时）
    │   └── ToolStatusBar（每个工具一行）
    │       ├── 运行中（running）：Loader2 旋转 + Wrench + 工具名 + 耗时
    │       ├── 已完成（completed）：CheckCircle2 绿色 + Wrench + 工具名 + 耗时 + 摘要
    │       └── 错误（error）：AlertCircle 红色 + Wrench + 工具名
    │
    ├── 思考区块（showThinking=true + 有 thinking 内容时）→ ThinkingBlock
    │   ├── [展开/折叠按钮] + "Thinking" 文字
    │   └── 折叠内容：Markdown 渲染（半透明）
    │
    ├── 工具调用卡片列表（历史消息中的 tool_use 块）
    │   └── ToolCard（每个工具一张卡）
    │       ├── CheckCircle2 绿色 + Wrench + 工具名（等宽字体）+ [展开/折叠]
    │       └── 展开内容：工具输入参数（JSON 格式化，等宽字体）
    │
    ├── 文字气泡（有文字时）→ MessageBubble
    │   ├── Markdown 全量渲染（remarkGfm 插件）
    │   │   ├── 代码块：pre + code（等宽字体，横向滚动）
    │   │   ├── 行内代码：浅色背景圆角
    │   │   └── 链接：新标签页打开
    │   └── 流式光标（isStreaming=true 时）：闪烁竖线
    │
    ├── 图片附件（来自 content blocks，位于文字下方）
    │   └── ImagePreviewCard → 点击放大 → ImageLightbox
    │
    ├── 文件附件（来自 _attachedFiles，位于文字下方）
    │   ├── 图片类 → ImagePreviewCard → 点击放大 → ImageLightbox
    │   ├── 图片类（无 preview）→ 占位框（File 图标）
    │   └── 非图片类 → FileCard
    │
    └── 悬停操作栏（有文字时显示，AssistantHoverBar）
        ├── 时间戳（相对时间格式）
        └── [复制按钮]（Copy 图标）→ 复制全文到剪贴板
            └── 复制成功状态：Check 绿色图标（2秒后恢复）

ImageLightbox（图片灯箱，Portal 到 document.body）
├── 半透明黑色背景遮罩（点击关闭）
├── 图片（最大 90vw × 85vh，object-contain）
├── [在文件夹中显示按钮]（仅有 filePath 时）→ shell:showItemInFolder
├── [关闭按钮] → onClose()
└── Escape 键关闭
```

---

## 数据流骨架

```
会话管理
  → 网关运行时自动加载：loadSessions() → sessions.list（IPC gateway:rpc）
  → 加载历史：loadHistory() → chat.history { sessionKey, limit:1000 }（IPC gateway:rpc）
  → 后台为每个非 main session 拉取首条消息用于侧边栏标签显示
  → 新会话：本地生成 key = agent:{id}:session-{timestamp}，不调用 sessions.reset
  → 切换会话：本地状态切换 + 触发 loadHistory()
  → 删除会话：IPC session:delete（重命名 .jsonl → .deleted.jsonl）+ 本地状态更新

发送消息
  → sendMessage(text, attachments?, targetAgentId?)
  → 附件先通过 /api/files/stage-paths 或 /api/files/stage-buffer 暂存到磁盘
  → 消息通过 WebSocket RPC 发送至 OpenAGI Gateway
  → 流式响应：handleChatEvent() 处理 delta / tool_use / completed 事件
  → 流式内容更新：streamingMessage / streamingTools / pendingFinal
  → 错误恢复：延迟定时器（避免误判 Gateway 内部重试为错误）
  → 中止：abortRun() → Gateway RPC

实时推送
  → 聊天事件通过 IPC renderer→ handleChatEvent() 注入 store
  → 事件去重（30 秒 TTL，基于 runId/sessionKey/seq 组合键）
  → 工具调用历史轮询（静默窗口 2.5 秒后启动，间隔轮询）

Trinity 双核博弈
  → POST /api/trinity/cycle/run { mode: 'debate' | 'competition' | 'refinement' }
  → 本地状态管理：trinityRunning / trinityResult（不进入全局 store）

文件处理
  → 原生文件选择：IPC dialog:open → POST /api/files/stage-paths（发送文件路径数组）
  → 粘贴/拖放：读取 File 对象 → base64 → POST /api/files/stage-buffer
  → 两种方式均返回 { id, fileName, mimeType, fileSize, stagedPath, preview }
  → 发送时仅传 stagedPath（磁盘路径引用），不传 base64
```

</details>

---

<details>
<summary><h2>4. 智能体 /agents ★★☆</h2></summary>

# Agents 页面 — 骨架树（去叶留枝版）

```
/agents
│
├── 页面级加载状态（首次加载，尚未完成初始拉取）
│   └── 全屏居中转圈动画（LoadingSpinner 大尺寸）
│
├── 页面头部
│   ├── 标题："Agents"
│   ├── 副标题："创建新的 Agent，可以将特定频道路由到不同的人格配置或工作区。"
│   ├── [刷新]（含旋转图标，刷新中时图标持续旋转）→ 触发 fetchAgents + fetchChannelAccounts
│   └── [添加 Agent]（含加号图标）→ 弹出「添加 Agent 对话框」
│
├── 警告横幅（Gateway 未运行时显示）
│   ├── 警告图标
│   └── "Gateway 服务未运行。Agent 或频道变更可能需要一点时间生效。"
│
├── 错误横幅（fetchAgents 失败时显示）
│   ├── 错误图标
│   └── 错误信息文本（来自 store error 字段）
│
├── Agent 卡片列表（每个 Agent 一行，静默刷新时保留旧数据）
│   └── 每张卡片
│       ├── Bot 图标
│       ├── Agent 名称
│       ├── "默认" 徽章（仅 isDefault=true 时显示，含勾号）
│       ├── 信息行1："Model: {modelDisplay}{（继承）}"（inheritedModel=true 时附加"（继承）"后缀）
│       ├── 信息行2："频道: {频道名称 · 账号名, ...}"（无绑定频道时显示"无"）
│       ├── [删除图标按钮]（仅 isDefault=false 时，悬停显示）→ 弹出「删除确认对话框」
│       └── [设置图标按钮]（isDefault 时始终显示，非默认时悬停显示）→ 弹出「Agent 设置弹窗」
│
├── 空状态（agents 列表为空且已完成初始加载）
│   └── （注：页面无显式空状态 UI，列表区域直接为空白）
│
├── ─── 弹窗/对话框 ───
│
├── 添加 Agent 对话框（showAddDialog=true 时显示）
│   ├── 标题："添加 Agent"
│   ├── 描述："输入名称即可创建新 Agent，可选择是否继承主 Agent 的工作区引导文件。"
│   ├── 表单
│   │   ├── 标签："Agent 名称"
│   │   ├── 输入框（占位符："Coding Helper"）
│   │   ├── 标签："继承主 Agent 工作区"
│   │   ├── 说明文字："从主 Agent 复制 SOUL.md、AGENTS.md 等引导文件"
│   │   └── 开关（Switch，默认关）→ 控制 inheritWorkspace 选项
│   └── 按钮区
│       ├── [取消] → 关闭对话框（不保存）
│       └── [保存]（名称为空时禁用；创建中时显示旋转图标 + "创建中..."）→ 调用 createAgent
│           ├── 成功 → 关闭对话框 + Toast "Agent 已创建"
│           └── 失败 → Toast "创建 Agent 失败：{error}"
│
├── 删除确认对话框（agentToDelete 不为空时显示）
│   ├── 标题："删除 Agent"
│   ├── 消息："确认从 OpenAGI 删除 "{name}"？这会永久删除该 Agent 及其由 OpenAGI 管理的工作区、运行时和会话文件。"
│   ├── [删除]（危险样式）→ 调用 deleteAgent
│   │   ├── 成功 → Toast "Agent 已删除"；若当前正在查看该 Agent 设置则关闭设置弹窗
│   │   └── 失败 → Toast "删除 Agent 失败：{error}"
│   └── [取消] → 关闭对话框
│
├── Agent 设置弹窗（activeAgent 不为空时显示）
│   ├── 标题："{agent.name} 设置"
│   ├── 描述："更新 Agent 名称，并管理哪些频道归属于这个 Agent。"
│   ├── [X 关闭按钮]
│   │   ├── 有未保存名称修改时 → 弹出「未保存修改确认对话框」
│   │   └── 无未保存修改时 → 直接关闭
│   │
│   ├── 基础信息区（两列网格）
│   │   ├── Agent 名称输入框
│   │   │   ├── （isDefault=true 时只读，无保存按钮）
│   │   │   └── （isDefault=false 时可编辑）
│   │   │       └── [保存按钮]（名称未改变或空时禁用；保存中显示旋转图标）→ 调用 updateAgent
│   │   │           ├── 成功 → Toast "Agent 已更新"
│   │   │           └── 失败 → Toast "更新 Agent 失败：{error}"
│   │   ├── Agent ID 信息块（只读）
│   │   │   ├── 标签："AGENT ID"
│   │   │   └── 文本：{agent.id}（等宽字体）
│   │   └── 模型信息块（可点击）→ 弹出「模型覆盖弹窗」
│   │       ├── 标签："MODEL"
│   │       ├── 文本：{agent.modelDisplay}{（继承）}
│   │       └── 副文本：{agent.modelRef 或 defaultModelRef 或 "-"}（等宽字体）
│   │
│   └── 频道区
│       ├── 区域标题："频道"
│       ├── 区域描述："该列表为只读。频道账号与绑定关系请在 Channels 页面管理。"
│       │
│       ├── 空状态（assignedChannels=[] 且 agent.channelTypes=[]）
│       │   └── "这个 Agent 还没有分配任何频道。"
│       │
│       ├── 频道卡片列表（assignedChannels 不为空时）
│       │   └── 每个频道卡片
│       │       ├── 频道 Logo（Telegram / Discord / WhatsApp / WeChat / DingTalk / Feishu / WeCom / QQ / 默认💬）
│       │       ├── 账号名称（主账号显示"主账号"）
│       │       ├── 频道类型名称 · 账号ID
│       │       └── 错误信息（lastError 不为空时，红色字体）
│       │
│       └── 仅有 channelTypes 但无 accountId 明细时
│           └── "该 Agent 绑定了频道类型，但具体账号绑定请在 Channels 页面查看。"
│
├── 模型覆盖弹窗（showModelModal=true 时显示，层级高于设置弹窗）
│   ├── 标题："Model"
│   ├── 描述："为该 Agent 选择 Provider 和模型 ID。"（含当前默认模型引用）
│   ├── [X 关闭按钮]
│   │   ├── 有未保存模型修改时 → 弹出「未保存修改确认对话框」
│   │   └── 无修改时 → 直接关闭
│   │
│   ├── Provider 选择下拉框
│   │   ├── 标签："Provider"
│   │   ├── 选项："选择 Provider"（占位）
│   │   ├── 选项列表：已启用且有有效凭证的账号（去重，按默认账号优先、更新时间倒序排列）
│   │   │   └── 格式："{账号标签} ({供应商名称})"
│   │   └── 空状态提示（无可用 Provider 时）
│   │       └── "尚未配置 Provider 账号。请先前往 设置 → AI Providers 添加。"（橙色警告文字）
│   │
│   ├── 模型 ID 输入框
│   │   ├── 标签："模型 ID"
│   │   └── 占位符：供应商配置的模型ID占位符 或 "model-id"
│   │
│   ├── 预览文本（nextModelRef 不为空时显示）
│   │   └── "预览: {provider}/{modelId}"（等宽字体）
│   │
│   └── 按钮区
│       ├── [使用默认模型]（无默认模型引用或已在使用默认模型时禁用）→ 将表单填入全局默认模型
│       ├── [取消] → 触发关闭流程
│       └── [保存]（未选 Provider / 模型ID为空 / 无修改时禁用；保存中显示旋转图标）→ 调用 updateAgentModel
│           ├── 成功（设置了覆盖）→ Toast "Agent 模型已更新" + 关闭弹窗
│           ├── 成功（重置为默认）→ Toast "Agent 模型已恢复为默认" + 关闭弹窗
│           └── 失败 → Toast "更新 Agent 模型失败：{error}"
│
└── 未保存修改确认对话框（两处共用：设置弹窗 + 模型覆盖弹窗）
    ├── 标题："未保存的修改"
    ├── 消息："你有未保存的修改。关闭后这些修改将被丢弃。"
    ├── [不保存并关闭]（确认）→ 丢弃修改，关闭上层弹窗
    └── [取消] → 回到上层弹窗继续编辑
```

---

## 数据流骨架

```
页面初始化
  → 并行执行：fetchAgents() + fetchChannelAccounts() + refreshProviderSnapshot()
  → 全部完成后设置 hasCompletedInitialLoad=true（期间显示全屏加载）
  → 若已有缓存数据（agents.length > 0）则 hasCompletedInitialLoad 初始值直接为 true（跳过全屏加载）

频道状态实时同步
  → 订阅 host 事件 'gateway:channel-status'
  → 事件触发时调用 fetchChannelAccounts()（自动更新卡片上的频道绑定信息）

Agent CRUD 操作
  → 创建：填写名称 + 可选继承工作区 → POST /api/agents → 返回最新快照 → 更新 store
  → 重命名：修改名称 → PUT /api/agents/{id} → 返回最新快照 → 更新 store
  → 更新模型：选择 Provider + 填写模型ID → PUT /api/agents/{id}/model → 更新 store
  → 删除：确认对话框 → DELETE /api/agents/{id} → 更新 store
  → 恢复默认模型：PUT /api/agents/{id}/model（modelRef=null）→ 更新 store
```

## Toast 消息汇总（共 14 种）

| 触发动作 | 成功 Toast | 失败 Toast |
|---------|-----------|-----------|
| 创建 Agent | "Agent 已创建" | "创建 Agent 失败：{error}" |
| 重命名 Agent | "Agent 已更新" | "更新 Agent 失败：{error}" |
| 删除 Agent | "Agent 已删除" | "删除 Agent 失败：{error}" |
| 更新模型（设置覆盖） | "Agent 模型已更新" | "更新 Agent 模型失败：{error}" |
| 更新模型（重置默认） | "Agent 模型已恢复为默认" | "恢复 Agent 默认模型失败：{error}" |
| 保存模型前校验失败 | — | "模型格式必须为 provider/model" |
| 保存模型前校验失败 | — | "请先选择 Provider" |
| 保存模型前校验失败 | — | "模型 ID 不能为空" |

</details>

---

<details>
<summary><h2>5. 消息频道 /channels ★★★</h2></summary>

# Channels 页面 — 骨架树（去叶留枝版）

```
/channels
│
├── 页面头部
│   ├── 标题："消息频道"
│   ├── 副标题："统一管理消息频道、账号、账号与智能体的绑定关系，以及频道默认账号"
│   └── [刷新] 按钮（网关未运行时禁用，刷新中显示旋转动画）
│
├── 全局警告横幅（网关未运行时显示）
│   └── 警告："网关未运行。未启用网关时无法管理频道。"
│
├── 错误横幅（加载失败时显示）
│   └── 错误消息文本
│
├── 已配置频道区（有已配置频道时显示）
│   ├── 区域标题："已配置频道"
│   │
│   └── 频道组卡片列表（每种频道类型一张卡片）
│       └── 频道组卡片
│           ├── 卡片头部
│           │   ├── 频道图标（Telegram / Discord / WhatsApp / WeChat / DingTalk / Feishu / WeCom / QQ Bot 专属图标，其他用 emoji）
│           │   ├── 频道名称（显示名，如 "Telegram"）
│           │   ├── 频道类型 ID（如 "telegram"，灰色小字）
│           │   ├── 连接状态指示点
│           │   │   ├── 绿色："connected（已连接）"
│           │   │   ├── 黄色闪烁："connecting（连接中）"
│           │   │   ├── 红色："error（异常）"
│           │   │   └── 灰色："disconnected（未连接）"
│           │   ├── [添加账号] 按钮 → 打开配置弹窗（新建账号模式）
│           │   └── [删除频道] 图标按钮 → 弹出确认对话框（删除整个频道组）
│           │
│           └── 账号列表（该频道下所有账号，每行一个）
│               └── 账号行
│                   ├── 账号显示名
│                   ├── 错误信息（有 lastError 时显示，红色）
│                   ├── "绑定 Agent（智能体）" 标签
│                   ├── Agent 绑定下拉选择器
│                   │   ├── 选项："未绑定"（空值）
│                   │   └── 选项列表：所有可用 Agent 名称（onChange → 立即调用绑定/解绑 API → Toast）
│                   ├── [编辑] 按钮 → 预拉取账号配置 → 打开配置弹窗（编辑模式）
│                   └── [删除账号] 图标按钮 → 弹出确认对话框（仅删除该账号）
│
├── 支持的频道区（始终显示，仅列出尚未配置的频道类型）
│   ├── 区域标题："支持的频道"
│   │
│   └── 频道卡片网格（2列，每个未配置的主频道类型一张卡片）
│       └── 频道卡片按钮（点击 → 打开配置弹窗）
│           ├── 频道图标
│           ├── 频道名称
│           ├── [插件] 徽章（isPlugin 为 true 时显示）
│           └── 频道描述文字（来自 i18n meta 描述）
│
├── 频道配置弹窗（点击频道卡片或"添加账号"/"编辑"后弹出）
│   ├── 弹窗头部
│   │   ├── 标题（三种模式）
│   │   │   ├── 新增频道（未选类型）："添加频道"
│   │   │   ├── 新增/配置："配置 {{频道名}}"
│   │   │   └── 编辑已有配置："更新 {{频道名}}"
│   │   ├── 副标题描述（三种情况）
│   │   └── [关闭 ×] 按钮 / 点击遮罩层关闭
│   │
│   ├── 第一步：频道类型选择网格（未预先指定类型时显示）
│   │
│   ├── 加载状态（预拉取已有配置中）
│   │   └── 加载动画 + 文字："正在加载配置..."
│   │
│   └── 第二步：配置表单（已选定频道类型后显示）
│       ├── 如何连接说明区
│       ├── 账号 ID 输入框（allowEditAccountId 为 true 时显示）
│       ├── 配置字段区（按各频道 configFields 定义动态渲染）
│       ├── 验证结果面板（点击"验证配置"后显示）
│       ├── 二维码显示区（connectionType 为 "qr"，点击生成后显示）
│       └── 操作按钮区
│           ├── [验证配置] 按钮（仅 token 类型 且 非 feishu 时显示）
│           └── 主操作按钮（根据状态变化）
│               ├── 二维码类型："生成二维码" / "正在生成二维码..."
│               └── Token 类型："保存并连接" / "更新并重新连接"
│
├── 删除确认对话框（点击任意删除按钮后弹出）
│   ├── 标题："Confirm（确认）"
│   ├── 消息（删除账号 / 删除频道）
│   ├── [删除] 按钮（危险样式）
│   └── [取消] 按钮
│
└── Toast 消息（操作反馈，共11种）
    ├── "{{name}} 连接成功" / "{{name}} 连接失败: {{error}}"
    ├── "频道 {{name}} 已保存"
    ├── "正在连接 {{name}}..."
    ├── "配置已保存，但刷新页面数据失败，请手动刷新查看最新状态"
    ├── "配置失败: {{error}}"
    ├── "账号绑定已更新"
    ├── "账号已删除"
    ├── "频道已删除"
    ├── "账号 ID 不能为空"
    └── "账号 ID {{accountId}} 已存在"
```

## 频道连接类型说明

```
连接类型    频道
──────────  ──────────────────────────────────────────────
token       Telegram / Discord / DingTalk / Signal /
            Feishu / WeCom / iMessage / Matrix / LINE /
            Microsoft Teams / Mattermost / QQ Bot
qr          WhatsApp / WeChat
webhook     Google Chat
```

</details>

---

<details>
<summary><h2>6. 技能 /skills ★★☆</h2></summary>

# Skills 页面 — 骨架树（去叶留枝版）

```
/skills
│
├── 页面头部
│   ├── 标题："技能"
│   ├── 副标题："浏览和管理 AI 能力"
│   └── [打开技能文件夹]（仅在有已安装技能时显示）
│
├── 网关警告横幅（网关未运行超过 1.5 秒后显示）
│   └── "网关未运行。没有活跃的网关，无法加载技能。"
│
├── 控制栏（搜索 + 筛选 + 批量操作 + 动作按钮）
│   ├── 搜索框（搜索范围：名称 / 描述 / ID / slug / 作者）
│   ├── 来源筛选标签组：[全部 (N)] / [内置 (N)] / [市场 (N)]
│   └── 右侧动作按钮组
│       ├── [批量启用可见项]
│       ├── [批量禁用可见项]
│       ├── [安装技能] → 打开安装面板（Sheet）
│       └── [刷新图标]（网关未运行时禁用）
│
├── 错误横幅（获取技能失败时显示）
│
├── 技能列表区（可滚动）
│   ├── 加载状态（初始加载中）
│   ├── 空状态（filteredSkills.length === 0）
│   └── 技能条目列表（排序：已启用 > 未启用；核心 > 非核心；字母序）
│       └── 每条技能行（点击 → 打开详情面板）
│           ├── 技能图标（emoji，默认 🧩）
│           ├── 技能名称
│           ├── 类型标识（🔒 核心 / 🧩 内置）
│           ├── slug 徽章
│           ├── 描述文字（单行截断）
│           ├── 底部元信息行（来源徽章 + baseDir 路径）
│           ├── 版本号 vX.X.X
│           └── 启用/禁用开关（核心技能禁用，不可操作）
│
├── 安装技能面板（Sheet，从右侧滑入）
│   ├── 头部（标题 + 搜索框 + 来源标签）
│   ├── 搜索中状态
│   ├── 搜索错误横幅
│   ├── 搜索结果列表（每条：图标/名称/作者/描述/版本 + [安装] 或 [卸载]）
│   └── 空提示区
│
└── 技能详情面板（Sheet，从右侧滑入，点击技能行触发）
    ├── 头部信息区（图标/名称/版本/类型/描述）
    ├── 来源区（路径 + [复制] + [打开目录]）
    ├── API 密钥区（非核心技能）
    ├── 环境变量区（非核心技能，支持增删）
    ├── 外部链接区（ClawHub / 打开手册）
    └── 底部操作按钮区（[保存配置] + [卸载]/[启用]/[禁用]）
```

## Toast 消息（操作反馈，共 16 种）

```
├── "技能已启用" / "技能已禁用"
├── "技能已安装并启用" / "技能已成功卸载"
├── "配置已保存" / "保存配置失败"
├── "路径已复制" / "复制路径失败"
├── "已在编辑器中打开" / "无法打开编辑器"
├── "无法打开技能文件夹"
├── "安装失败" / "卸载失败"
├── "技能文件夹尚不存在，请先安装一个技能。"
├── 批量操作反馈："已启用 N 个技能" / "已禁用 N 个技能" / 部分失败等
└── 搜索/安装网络错误（带手动安装路径提示）
```

## 数据流骨架

```
技能列表数据（三路合并）
  → Gateway RPC skills.status → 运行中技能状态
  → Host API /api/clawhub/list → 磁盘已安装技能
  → Host API /api/skills/configs → 本地技能配置

技能操作
  → 启用/禁用：Gateway RPC skills.update → 本地 store 更新
  → 安装：Host API /api/clawhub/install → 自动 enableSkill → fetchSkills
  → 卸载：Host API /api/clawhub/uninstall → fetchSkills
  → 保存配置：IPC skill:updateConfig（apiKey + env）→ fetchSkills
```

</details>

---

<details>
<summary><h2>7. 定时任务 /cron ★★☆</h2></summary>

# Cron 页面 — 骨架树（去叶留枝版）

```
/cron
│
├── 页面头部
│   ├── 标题："定时任务"
│   ├── 副标题："通过定时任务自动化 AI 工作流"
│   ├── [刷新] 按钮（网关未运行时禁用）
│   └── [新建任务] 按钮（网关未运行时禁用）
│
├── 告警区（条件）
│   ├── 网关未运行警告
│   └── 加载错误提示
│
├── 统计卡片栏（4 列）
│   ├── 任务总数
│   ├── 运行中（enabled=true）
│   ├── 已暂停（enabled=false）
│   └── 失败（lastRun.success=false）
│
├── 任务列表区
│   ├── 加载中状态
│   ├── 空状态
│   └── 任务卡片列表（两列网格）
│       └── 任务卡片（CronJobCard）
│           ├── 点击卡片 → 打开编辑弹窗
│           ├── 头部区域（图标/名称/状态/调度计划/开关）
│           ├── 消息预览（最多显示 2 行）
│           ├── 元数据区（投递通道 / 上次运行 / 下次运行）
│           ├── 上次运行错误（有时显示）
│           └── 操作按钮（[立即运行] + [删除]）
│
├── 创建/编辑弹窗（TaskDialog）
│   ├── 头部（标题 + 副标题 + 关闭按钮）
│   ├── 表单字段
│   │   ├── 任务名称
│   │   ├── 消息/提示词（多行文本框）
│   │   ├── 调度计划（预设8个 / 自定义 Cron 表达式）
│   │   ├── 投递设置（仅在 OpenAGI 内 / 发送到外部通道）
│   │   │   └── 外部通道配置区（通道选择 / 账号 / 接收目标）
│   │   └── 立即启用开关
│   └── 操作按钮（[取消] + [创建任务]/[保存更改]）
│
└── 删除确认对话框
    ├── 标题："确认"
    ├── 内容："确定要删除此任务吗？"
    ├── [删除]（危险样式）→ Toast "任务已删除"
    └── [取消]
```

## Toast 消息汇总（共 13 种）

```
操作类：任务已创建 / 已更新 / 已启用 / 已暂停 / 已删除 / 已成功触发
错误类：触发失败 / 更新失败 / 删除失败 / 加载投递目标失败 / 解析默认投递目标失败
校验类：请输入任务名称 / 请输入消息 / 请选择调度计划 / 请选择通道 / 请输入投递目标 / 已解析投递目标
```

## 数据流骨架

```
页面挂载 → fetchJobs()（/api/cron/jobs） + fetchConfiguredChannels()

任务操作
  → 创建：POST /api/cron/jobs
  → 编辑：PUT /api/cron/jobs/:id
  → 删除：DELETE /api/cron/jobs/:id
  → 开关：POST /api/cron/toggle
  → 立即运行：POST /api/cron/trigger
```

</details>

---

<details>
<summary><h2>8. 设置 /settings ★★★</h2></summary>

# Settings 页面 — 骨架树（去叶留枝版）

```
/settings
│
├── 页面头部
│   ├── 标题："设置"
│   └── 副标题："配置您的 OpenAGI 体验"
│
├── 顶部标签页导航
│   ├── [通用设置]（默认激活）
│   └── [AI 引擎]
│
├── ══════ 标签页 A：通用设置 ══════
│
│   ├── 区块一：通用（主题 / 语言 / 开机自启）
│   ├── 区块二：网关（状态/重启/日志/自启/开发者模式/匿名遥测）
│   ├── 区块三：开发者（仅 devModeUnlocked=true 时渲染）
│   │   ├── Gateway Proxy（代理设置）
│   │   ├── 网关令牌（只读 + 复制）
│   │   ├── OpenAGI CLI（复制命令）
│   │   ├── OpenAGI Doctor 诊断
│   │   ├── WS 诊断模式开关
│   │   └── 埋点查看器
│   ├── 区块四：更新（版本/状态/进度/自动更新开关）
│   └── 区块五：关于（版本/官网/GitHub/FAQ 链接）
│
└── ══════ 标签页 B：AI 引擎 ══════
    ├── 区块一：AI 代码引擎状态卡（状态指示 + 启用/禁用开关）
    ├── 区块二：AI 模型选择（4种模型卡片网格）
    ├── 区块三：API 密钥（Anthropic API Key）
    ├── 区块四：工作目录
    ├── 区块五：高级参数（最大回合数 / 最大输出 Token）
    ├── 区块六：工具能力（14种工具卡片，点击切换启用/禁用）
    └── 保存按钮（底部固定）→ Toast "AI 引擎配置已保存"
```

## Toast 消息汇总（共 20 种）

```
通用设置页
├── 代理设置相关（2种）
├── 网关令牌已复制 / CLI 命令已复制
├── OpenAGI doctor 诊断相关（6种：完成/发现问题/失败×2模式）
├── 诊断输出已复制
├── WS 诊断模式开关（2种）
└── 埋点已复制 / 埋点已清空

AI 引擎标签页
├── "AI 引擎配置已保存"
└── "保存失败: {err}"

系统事件
└── "openagi CLI installed at {path}"
```

## 数据来源

```
useSettingsStore   → 主题 / 语言 / 开机启动 / 网关自启 / 代理 / 开发者模式 / 遥测 / 自动更新
useGatewayStore    → 网关状态（state/port）/ restart()
useUpdateStore     → 版本 / 更新状态 / 进度 / 错误 / 倒计时
useCodeEngineStore → 引擎运行状态
IPC                → CLI命令 / 日志 / Doctor诊断 / AI引擎配置
```

</details>

---

<details>
<summary><h2>9. 初始化向导 /setup ★★☆</h2></summary>

# Setup 页面 — 骨架树（去叶留枝版）

```
/setup
│
├── 顶部进度指示器（5 步骤圆点 + 连接线）
│   └── 步骤列表
│       ├── 步骤0："欢迎使用 OpenAGI"
│       ├── 步骤1："环境检查"
│       ├── 步骤2："AI 提供商"
│       ├── 步骤3："设置中"
│       └── 步骤4："准备就绪！"
│
├── 步骤内容区（按当前步骤切换，带滑动动画）
│   ├── 步骤0：欢迎页（Logo / 标题 / 语言选择器 / 产品特性列表）
│   ├── 步骤1：环境检查
│   │   ├── Node.js 运行时（✓/✗）
│   │   ├── OpenAGI 包（✓/✗ + 路径说明）
│   │   └── 网关服务（✓/✗ + [启动网关] 按钮）
│   │   └── 日志面板（点击展开）
│   ├── 步骤2：AI 提供商（选择提供商 → 配置 → 验证）
│   │   ├── 提供商选择下拉框
│   │   ├── 动态配置区（API Key / OAuth 流程）
│   │   └── 验证结果提示
│   ├── 步骤3：安装中（自动执行，显示进度条 + 5个技能安装状态）
│   └── 步骤4：完成（🎉 汇总卡片 + [开始使用]）
│
└── 底部导航栏（步骤3时隐藏）
    ├── [返回]（非第一步时显示）
    ├── [跳过设置]（非最后步、非步骤1时）
    └── [下一步 →] / [开始使用]
```

## Toast 消息汇总

```
├── "提供商配置成功" / "无效的 API 密钥"
├── "Configuration failed: ..."
├── "Environment setup failed" / "Installation error"
├── "Code copied to clipboard"
├── "设置完成！"
└── "不能同时添加 MiniMax 国际站和国内站的服务商。"
```

## 步骤流转图

```
步骤0 欢迎 → [下一步] → 步骤1 环境检查
步骤1 → 三项全通过 → [下一步] → 步骤2 AI 提供商
步骤2 → 验证通过 → [下一步] → 步骤3 安装中（自动执行）
步骤3 → 安装完成（延迟1秒）→ 步骤4 完成
步骤4 → [开始使用] → markSetupComplete() → /
任意步骤 → [跳过设置] → /
```

</details>

---

<details>
<summary><h2>10. 实例管理 /instances ★☆☆</h2></summary>

# Instances 页面 — 骨架树（去叶留枝版）

```
/instances
│
├── 页面头部
│   ├── 标题："实例管理"
│   ├── 副标题："查看和管理运行中的服务实例"
│   └── [刷新] 按钮（加载中时图标旋转 + 按钮禁用）
│
├── 实例列表区
│   ├── 有实例时：实例卡片列表
│   │   └── 每张卡片
│   │       ├── 服务器图标
│   │       ├── 实例名称（如"OpenAGI 网关"、"Claude Code 引擎"）
│   │       ├── 状态徽章（"运行中"绿点 / "已停止"灰点 / "错误"红点）
│   │       └── 元数据信息行（端口 / PID / 内存占用 / 启动时间）
│   │
│   └── 无实例时：空状态
│       ├── 警告图标
│       └── 提示文字："暂无运行中的实例"
│
└── 数据刷新机制（无UI，后台运行）
    ├── 页面挂载时：立即拉取一次
    └── 定时轮询：每 10 秒自动刷新
```

## 数据流骨架

```
页面挂载
  → 调用 /api/gateway/instances
  → 成功：渲染返回的实例列表
  → 失败（API 异常）：渲染默认兜底实例（网关 + Code 引擎）
  → 每 10 秒重复上述流程
```

## 特殊说明

- 无 Toast 消息、无弹窗、纯展示型页面
- uptime / cpuPercent 字段已定义但当前未渲染
- 无实例操作按钮（启动/停止/重启）

</details>

---

<details>
<summary><h2>11. 会话管理 /sessions ★☆☆</h2></summary>

# Sessions 页面 — 骨架树（去叶留枝版）

```
/sessions
│
├── 页面头部
│   ├── 标题："会话管理"
│   ├── 副标题："查看和管理所有会话 · N 个会话"
│   └── [刷新] 按钮
│
├── 搜索栏
│   └── 实时过滤字段：key / agentName / channelType / displayName
│
├── 会话列表区
│   ├── 加载中状态
│   ├── 空状态："暂无会话记录"
│   ├── 搜索无结果状态："没有匹配的会话"
│   └── 会话卡片列表
│       └── 每张卡片
│           ├── MessageSquare 图标
│           ├── 会话名称（displayName 或 key）
│           ├── 状态徽章（"活跃" / "空闲" / "已完成"）
│           ├── 元信息行（智能体 / 渠道类型 / 消息数 / 最后活跃时间）
│           └── （仅 status=active 时）[终止] 按钮 → 弹出确认对话框
│
├── 终止会话确认对话框
│   ├── 标题："终止会话"
│   ├── 内容："确定要终止会话「名称/key」吗？这将立即中断正在进行的对话。"
│   ├── [取消]
│   └── [终止]（危险样式）→ POST /api/gateway/sessions/{key}/kill
│
└── Toast 消息（共 2 种）
    ├── 成功："会话 {名称/key} 已终止"
    └── 失败："终止失败: {错误信息}"
```

## 数据流骨架

```
会话数据拉取
  → GET /api/gateway/sessions
  → 首次挂载立即拉取 + 每 8 秒自动轮询
  → 拉取失败静默处理

终止会话流程
  → 点击 [终止] → 确认对话框
  → 确认 → POST /api/gateway/sessions/{key}/kill
  → 成功：Toast + 重新拉取
  → 失败：Toast（含错误信息）
```

</details>

---

<details>
<summary><h2>12. 文档 /documents ★☆☆</h2></summary>

# Documents 页面 — 骨架树（去叶留枝版）

```
/documents
│
├── 页面头部
│   ├── 标题："文档"
│   ├── 副标题："知识库文档与配置文件 · N 个文件"
│   └── [刷新] 按钮
│
├── 搜索与过滤区
│   ├── 搜索输入框（placeholder："搜索文档名称或描述..."）
│   └── 类型过滤器（标签切换组）
│       ├── [全部]
│       ├── [技能]（有数量时显示计数）
│       ├── [智能体]（有数量时显示计数）
│       └── [配置]（有数量时显示计数）
│
├── 文档列表区
│   ├── 有匹配文档时：文档卡片列表
│   │   └── 每张卡片
│   │       ├── 类型图标（skill→代码 / agent→书本 / config→文件 / other→文本）
│   │       ├── 文件名称（截断处理）
│   │       ├── 类型徽章（技能文件 / 智能体 / 配置文件 / 其他）
│   │       ├── 元数据行（路径 / 文件大小 / 最后修改时间）
│   │       └── 文件描述（有 description 字段时显示）
│   │
│   └── 空状态
│       ├── 警告图标
│       ├── "没有匹配的文档" / "暂无文档"
│       └── "文档会在配置智能体和技能后自动出现"
│
└── 数据状态
    ├── 加载中：刷新按钮图标旋转
    └── 加载完成：显示文档列表或空状态
```

## 数据流骨架

```
文档数据
  → 页面挂载时自动请求：GET /api/gateway/documents
  → 点击 [刷新] 按钮手动重新请求
  → 请求失败：静默处理，显示空列表
  → 前端过滤（类型 + 搜索词）→ 渲染列表

无外部 Store 依赖 / 无 Toast / 无弹窗 / 无分页
```

</details>

---

<details>
<summary><h2>13. 使用情况 /usage ★☆☆</h2></summary>

# Usage 页面 — 骨架树（去叶留枝版）

```
/usage
│
├── 页面头部
│   ├── 标题："使用情况"
│   ├── 副标题："Token 用量、API 调用和费用统计"
│   └── [刷新] 按钮（加载中时旋转动画 + 禁用）
│
├── 统计总览区（四格卡片，2列/4列响应式）
│   ├── 卡片①：总 Token（格式化 K/M，含输入/输出明细）
│   ├── 卡片②：API 调用（累计请求次数）
│   ├── 卡片③：预估费用（"$0.00"，基于 Token 估算）
│   └── 卡片④：模型数（已使用的模型种数）
│
└── 模型用量明细区
    ├── 区域标题："模型用量明细"
    ├── 有数据时：模型行列表（按总 Token 降序）
    │   └── 每行（模型名称 / 提供商徽章 / Token数 / 进度条 / 调用次数 / 百分比）
    └── 空状态："暂无使用数据"
```

## 数据流骨架

```
用量数据
  → 页面挂载时立即拉取 /api/gateway/usage
  → 每 30 秒自动刷新
  → 拉取失败时降级为零值
  → 页面卸载时清除定时器

数值格式化规则：≥1,000,000 → N.NM / ≥1,000 → N.NK / 其余直接显示整数
```

## 特殊说明

- 无 i18n 依赖，所有文本硬编码中文
- 无全局 store 依赖，直接通过 hostApiFetch 调用网关 API
- 无弹窗、无 Toast、无表单交互

</details>

---

<details>
<summary><h2>14. Trinity /trinity ★★★★</h2></summary>

# Trinity 页面 — 骨架树（去叶留枝版）

```
/trinity
│
├── 前置条件：创世状态检测
│   └── (创世未完成时) → 显示创世序列屏幕，隐藏所有其他内容
│
├── ══════════════════════════════════════════
│   子页面导航（路由切换，主侧边栏实现）
│   ├── [Trinity 控制台]  /trinity
│   ├── [任务目标]        /trinity/goal
│   ├── [治理账本]        /trinity/governance
│   ├── [知识市场]        /trinity/market
│   ├── [蜂群网络]        /trinity/swarm
│   ├── [区块链 & 经济]   /trinity/blockchain
│   └── [Trinity 设置]    /trinity/settings
│
├── ══════════════════════════════════════════
│   创世序列屏幕（仅 genesisStatus.isComplete === false 时显示）
│   ├── 标题："创世序列"
│   ├── 密码短语输入框（密码类型，至少8位）
│   ├── 确认密码短语输入框
│   └── [激活节点]（禁用条件：密码<8位 或 两次不一致 或加载中）
│
├── ══════════════════════════════════════════
│   一、Trinity 控制台（/trinity，主仪表盘）
│   ├── 页面头部（标题 + 节点ID + 阶段徽章 + 轮次 + 游戏模式 + [刷新]）
│   ├── 顶部统计卡片（4格：New.B余额 / 信用分 / PoO已验证 / 平均分数）
│   ├── 三体治理角色区（AI-1扩张者 / AI-2风控员 / AI-3财务官）
│   ├── 经济摘要区（总收入 / 总支出 / 已质押 / 待处理债务）
│   ├── 最近交易表格（时间/类型/金额/余额）
│   └── 底部双栏（置信度进度条 + 成功经验库统计）
│
├── ══════════════════════════════════════════
│   二、任务目标（/trinity/goal）
│   ├── 页面头部（[运行一次] / [启动/停止自动]）
│   ├── 运行器状态卡片（状态灯 / 统计 / 暂停原因 / 错误）
│   ├── 当前目标卡片（目标/优先级/子目标 + [完成] + [放弃]）
│   ├── 空状态 + [设定目标] → 展开新建表单
│   ├── 新建目标表单（标题/描述/优先级/指标/截止日期）
│   ├── 历史记录区（completed / abandoned 状态标签）
│   └── 确认对话框：放弃目标
│
├── ══════════════════════════════════════════
│   三、治理账本（/trinity/governance）
│   ├── 标签栏（证据链 / 战略债务 / 成功经验库 / PoO 验证任务）
│   ├── 证据链标签（H1-H4等级徽章 + 哈希 + 声明）
│   ├── 战略债务标签（[运行联邦清算] + 债务卡片 + [解决]）
│   ├── 成功经验库标签（成功率 / 使用次数 / 标签徽章）
│   └── PoO 验证任务标签（5格统计 + 任务列表 + 分数徽章）
│
├── ══════════════════════════════════════════
│   四、知识市场（/trinity/market）
│   ├── 标签栏（先知挖矿 / 知识市场）
│   ├── 先知挖矿标签
│   │   ├── 5格统计摘要
│   │   ├── [创建预测] → 展开表单（声明/类别/指标/预测值/日期/质押/置信度）
│   │   └── 预测列表（状态/类别/准确率/质押/声明/元信息）
│   └── 知识市场标签
│       ├── 4格统计摘要
│       └── 商品列表（标题/状态/等级/描述/标签/价格/截止 + [购买]确认对话框）
│
├── ══════════════════════════════════════════
│   五、蜂群网络（/trinity/swarm）
│   ├── 页面头部（[发现节点] / [刷新] / [启动/停止蜂群]）
│   ├── 蜂群状态卡片（状态灯 + 5格统计）
│   ├── 连接节点表单（地址 + 端口 + [连接]）
│   ├── 已连接节点表格（节点ID/地址/状态/信用分/延迟/最后在线）
│   └── 联邦防御区块（[举报节点] → 展开举报表单）
│
├── ══════════════════════════════════════════
│   六、区块链 & 经济（/trinity/blockchain）
│   ├── 标签栏（区块链 / 汇率 / 分红）
│   ├── 区块链标签（初始化状态 / 已初始化：4格统计 + 区块列表）
│   ├── 汇率标签（汇率面板 / 24h统计 / 货币转换器）
│   └── 分红标签（3格摘要 / 配置编辑 / [执行支付]确认对话框 / 记录列表）
│
└── ══════════════════════════════════════════
    七、Trinity 设置（/trinity/settings）
    ├── 页面头部（[保存]）
    ├── AI 供应商配置区（提供商下拉 / 模型 / 基础URL）
    ├── 自动运行器配置区（循环间隔 / 最大失败次数）
    ├── PoO 验证配置区（执行阈值 / 沙盒超时 / 置信度暂停线）
    ├── 三体 AI 人格设置区（AI-1/2/3 描述文本框 + 温度滑块）
    └── 当前约束条件区（严重程度徽章 + 规则内容 + 类别徽章）
```

## Toast 消息汇总（共28种）

```
目标操作：目标创建成功 / 目标已完成 / 目标已放弃 / 子目标已添加
运行器操作：自动运行器已启动 / 已停止 / 单次运行完成
治理操作：联邦清算执行完成 / 债务已解决
区块链操作：区块链初始化成功 / 汇率重新计算完成 / 分红配置已保存 / 分红支付已执行
知识市场操作：预测创建成功 / 购买成功
蜂群操作：蜂群已启动 / 已停止 / 节点连接成功 / 节点发现已触发 / 举报已提交
设置操作：设置已保存
通用错误：数据加载失败 / 操作失败 / 错误: ...（动态拼接）
输入验证：端口范围需在1-65535之间 / 质押金额必须大于0
```

</details>

---

<details>
<summary><h2>15. 经济 /trinity（Economy 嵌套） ★★☆</h2></summary>

# Economy 页面 — 骨架树（去叶留枝版）

> **说明**：`/src/pages/Economy/` 目录当前为空壳（仅存在目录，无任何文件）。
> "经济"功能现阶段完全嵌套于 Trinity 控制台（`/src/pages/Trinity/`）中，
> 分布在仪表板总览、区块链 & 经济、治理账本、知识市场四个子页面。
> 本骨架树按实际已实现功能进行完整梳理。

---

```
/trinity（Trinity 控制台 — 经济相关功能）
│
├── 前置：创世序列（Genesis）屏
│   （条件：genesisStatus.isComplete === false）
│   ├── 密码短语输入框（password 类型，≥8位）
│   ├── 确认密码短语输入框（password 类型）
│   ├── 校验规则：两次输入一致 + 长度≥8 才启用按钮
│   └── [激活节点]（提交中禁用）
│       └── 成功 → checkGenesis() + fetchDashboard() → 进入主仪表板
│
├── ── A. Trinity 仪表板总览（Dashboard）──────────────────────────────
│   ├── 页面头部（标题 / 节点ID / 阶段/轮次/博弈模式徽章 / [刷新]）
│   ├── 顶部四格统计卡（New.B余额 / 信用分 / PoO已验证 / 平均分数）
│   ├── 三体治理角色区（AI-1扩张者 / AI-2风控员 / AI-3财务官）
│   ├── 经济摘要区（总收入绿 / 总支出红 / 已质押蓝 / 待处理债务橙）
│   ├── 最近交易列表（时间/类型/金额正负色/余额）
│   └── 置信度 & 治理统计区（进度条 + 成功经验库数字）
│
├── ── B. 区块链 & 经济（BlockchainPage）──────────────────────────────
│   ├── Tab：区块链（未初始化 → [初始化] / 已初始化：4格统计 + 区块列表）
│   ├── Tab：汇率（汇率面板 / 24h统计 / 货币转换器）
│   └── Tab：分红（3格摘要 / 配置编辑 / [执行支付]确认对话框 / 记录列表）
│
├── ── C. 治理账本（GovernancePage）───────────────────────────────────
│   ├── Tab：证据链（H1-H4等级 / 来源 / 已持久化 / 声明 / 哈希）
│   ├── Tab：战略债务（[运行联邦清算]确认 / 严重度 / [解决]按钮）
│   ├── Tab：成功经验库（成功率 / 使用次数 / 标签徽章）
│   └── Tab：PoO 验证任务（5格统计 / 状态图标 / 分数徽章 / New.B奖励）
│
└── ── D. 知识市场（MarketPage）─────────────────────────────────────
    ├── Tab：先知挖矿（5格统计 / 创建预测表单 / 预测列表）
    └── Tab：知识市场（4格统计 / 商品列表 + [购买]确认对话框）
```

---

## 经济数据结构骨架

```
TrinityEconomy（钱包状态）
  ├── balance：当前 New.B 余额
  ├── totalEarned：历史总收入
  ├── totalSpent：历史总支出
  ├── totalStaked：已质押金额
  ├── halvingEpoch：当前减半纪元
  ├── currentRewardRate：当前每任务奖励率
  └── recentTransactions[]：最近交易记录
```

## Toast 消息汇总（共13种）

```
区块链相关：区块链初始化成功 / 汇率重新计算完成 / 分红配置已保存 / 分红支付已执行
治理相关：联邦清算执行完成 / 债务已解决
知识市场相关：预测创建成功 / 购买成功
校验错误：质押金额必须大于0 / predictedValue must be a number / confidence must be 1-100
通用：数据加载失败 / 操作失败
```

## 当前实现状态说明

```
/src/pages/Economy/     → 空目录（占位，暂无实现）
/src/pages/Trinity/     → 经济功能实际承载位置
  ├── index.tsx         → 仪表板总览（含余额、交易、债务摘要）
  ├── BlockchainPage.tsx → 区块链 + 汇率 Oracle + 分红管理
  ├── GovernancePage.tsx → 证据链 + 战略债务 + 经验库 + PoO 任务
  └── MarketPage.tsx    → 先知挖矿预测 + 知识资产拍卖市场
```

</details>

---

*文件由文档合并专员自动生成 · 2026-04-12*
